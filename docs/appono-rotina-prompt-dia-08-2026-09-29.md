# Prompt do Dia 8 - Metricas e relatorio comparativo da Appono.AI

Voce e um agente senior de engenharia de software, experimentacao, estatistica aplicada, sistemas de recomendacao, seguranca e desenvolvimento full-stack trabalhando diretamente no projeto Appono. Execute integralmente o marco de **29 de setembro de 2026** do calendario de entrega da Appono Intelligence V2, apresentada publicamente como **Appono.AI**.

O objetivo de hoje e **validar, agregar e comparar os resultados longitudinais prospectivos de desenvolvimento e validacao**, produzindo metricas por modelo, semana e persona sem alterar formulas, reexecutar a reserva ou confundir divergencia com qualidade.

`Appono.AI` e o nome publico do motor. Identificadores tecnicos congelados como `appono-intelligence-v1`, `appono-intelligence-v2`, nomes de arquivos, hashes, protocolos e migrations nao devem ser renomeados, pois fazem parte da rastreabilidade experimental.

Nao entregue apenas analise, planilha manual, pseudocodigo ou uma conclusao escrita sem evidencia executavel. Inspecione o estado real do repositorio, valide os relatorios brutos, implemente um agregador puro, adicione testes, produza relatorios canonicos e escreva um checkpoint datado. Nao faca commit, push, deploy ou migration remota sem autorizacao explicita.

## Marco do calendario

Data planejada: `29/09/2026`.

Entrega prevista:

> Metricas e relatorio comparativo: utilidade, arrependimento, diversidade, concentracao e confianca por modelo.

O marco somente pode ser encerrado quando estiver comprovado que:

- os relatorios brutos de desenvolvimento e validacao correspondem aos hashes congelados;
- todas as 1.800 execucoes de modelo possuem chave unica e integridade verificavel;
- escolhas foram associadas aos candidatos corretos dos snapshots;
- desenvolvimento e validacao foram agregados separadamente;
- metricas existem por conjunto, modelo, persona e semana;
- utilidade e arrependimento possuem denominadores explicitos;
- diversidade, repeticao e concentracao foram calculadas a partir de restaurante, produto e categoria reais da fixture sintetica;
- confianca, amostras efetivas, volume e consistencia nao foram preenchidos artificialmente quando ausentes;
- fallback e falha nao receberam credito de escolha nativa;
- criterios congelados em 22/09 foram avaliados sem flexibilizacao;
- regressao grave por persona nao foi escondida por media global;
- nenhum arquivo bruto, snapshot ou formula foi alterado;
- nenhuma reserva foi acessada;
- nenhum resultado sintetico foi apresentado como validacao comercial.

## Pre-condicao do Dia 28

Antes de editar ou agregar resultados, confirme:

- decisao de 28/09 igual a `SIMULACAO_LONGITUDINAL_CONCLUIDA`;
- checkpoint de 28/09 presente e coerente;
- protocolo `appono-intelligence-longitudinal-prospective-v1`;
- executor `routine-longitudinal-simulation-v1`;
- schema bruto `routine-longitudinal-raw-report-v1`;
- controle `deterministico-v3`;
- referencia `appono-intelligence-v1`;
- desafiante `appono-intelligence-v2`;
- regua externa `persona-utility-v1`;
- 300 cenarios e 900 execucoes por conjunto;
- zero falhas, zero fallbacks e zero violacoes eliminatorias nas execucoes reais;
- neutralidade da V2 sem historico aprovada;
- relatorio de desenvolvimento com conteudo SHA-256 `e71c128fa04d567182497d621c32c9eb22fd8783f9e3560300662d6926f02b57`;
- relatorio de validacao com conteudo SHA-256 `3a6cdb5121f28a337a9448e14382de91a6b0506933be5dd126002edd1328c7fc`;
- arquivo de desenvolvimento SHA-256 `b8033dc6d5b15e40e4ad2acc15897c65c451828953f1112b105a06f382718467`;
- arquivo de validacao SHA-256 `1abfe85103adc84baf76735380eeb6eb7443b99519608c49e7dedc133669ecee`;
- manifesto dos relatorios SHA-256 `7223109f3f804805b6362ad7ef07b270808bd20f4e26d2622bdabc67fae36288`;
- snapshots de desenvolvimento e validacao com os hashes historicos preservados;
- reserva prospectiva `SEALED_UNMATERIALIZED`;
- rollout publico igual a zero.

Se qualquer hash ou contagem divergir, interrompa a avaliacao e investigue. Nao regenere ou sobrescreva relatorios para ocultar a divergencia.

## Estado conhecido a confirmar

Trate os itens abaixo como hipoteses que devem ser verificadas:

- desenvolvimento possui 30 divergencias de escolhas dos desafiantes contra o controle;
- validacao possui 47 divergencias;
- as divergencias nao foram classificadas como vitoria ou derrota;
- os relatorios guardam candidato escolhido, utilidade, arrependimento, reacao, confianca e hashes de estado;
- restaurante, produto e categoria da escolha devem ser recuperados por juncao com o snapshot congelado;
- cada cenario possui quatro candidatos elegiveis;
- as trajetorias dos modelos podem divergir depois da primeira escolha;
- nao existe avaliacao comparativa prospectiva consolidada;
- nenhuma formula foi modificada depois da validacao.

Registre divergencias entre essa descricao e o estado real. Nao corrija documentacao para esconder defeito de dado.

## Leitura obrigatoria

Antes de editar, leia integralmente:

- `README.md`;
- `package.json`;
- `backend/package.json`;
- `frontend/package.json`;
- `docs/appono-rotina-calendario-ia-ate-2026-10-10.md`;
- checkpoints de 22 a 28/09;
- prompts dos Dias 1 a 7;
- `docs/appono-rotina-prompt-pre-piloto-intelligence-v2.md`;
- `backend/experiments/routine-intelligence/manifest.json`;
- `backend/experiments/routine-intelligence/personas-v1.json`;
- `backend/experiments/routine-intelligence/partitions-v1.json`;
- `backend/experiments/routine-intelligence/longitudinal-protocol-v1.json`;
- `backend/experiments/routine-intelligence/scenarios/manifest-v1.json`;
- snapshots congelados de desenvolvimento e validacao;
- `backend/reports/routine-intelligence/prospective/manifest-v1.json`;
- relatorios brutos prospectivos de desenvolvimento e validacao;
- `backend/src/domain/routine-intelligence-personas.js`;
- `backend/src/domain/routine-intelligence-longitudinal-contract.js`;
- `backend/src/domain/routine-intelligence-longitudinal-simulation.js`;
- `backend/src/domain/routine-intelligence-simulation.js`, apenas como referencia historica;
- `backend/src/domain/routine-shadow-evaluation.js`;
- scripts de simulacao e avaliacao existentes;
- testes de manifesto, personas, simulacao, contrato e executor longitudinal.

Este marco nao exige Supabase, banco remoto, autenticacao, migration, rota HTTP ou frontend funcional.

## Regras inegociaveis

1. Preserve todas as alteracoes locais e artefatos congelados.
2. Nao altere controle, V1, V2, utilidade das personas, pesos, limites ou desempates.
3. Nao reexecute os modelos para produzir um resultado diferente.
4. E permitido executar as CLIs do Dia 28 somente em `--check`.
5. Nao sobrescreva relatorios brutos prospectivos.
6. Nao altere snapshots para facilitar a juncao.
7. Nao misture desenvolvimento e validacao em uma unica media principal.
8. Nao use desenvolvimento para declarar desempenho de validacao.
9. Nao exclua persona, semana, cenario, falha ou empate desfavoravel.
10. Nao credite fallback como decisao nativa.
11. Nao substitua valor ausente por zero quando zero possuir significado estatistico.
12. Nao use confianca do modelo como utilidade externa.
13. Nao use score do modelo como medida de qualidade entre modelos diferentes.
14. Nao compare score bruto da V1 com score bruto da V2 como se fossem calibrados na mesma escala.
15. Nao interprete divergencia isolada como melhora ou regressao.
16. Nao flexibilize criterios congelados depois de observar os numeros.
17. Nao abra, leia, materialize, reconstrua ou inferir a reserva prospectiva.
18. Nao reexecute a reserva historica.
19. Nao exponha PII, credenciais, sinais individuais sensiveis ou material da reserva.
20. Nao habilite rollout publico nem altere sugestoes visiveis.
21. Nao faca commit, push, deploy, pagamento, e-mail, seed ou migration remota.

## Escopo permitido

Este marco pode:

- validar qualidade e integridade dos relatorios brutos;
- implementar agregador de metricas puro;
- juntar escolhas aos snapshots congelados;
- calcular metricas por conjunto, modelo, persona e semana;
- comparar utilidade externa e arrependimento no mesmo cenario;
- calcular diversidade, repeticao, concentracao e cobertura de preferencia;
- analisar confianca, volume efetivo, consistencia e tamanho da amostra;
- produzir matriz de vitorias, derrotas e empates pela regua externa;
- avaliar criterios de aceitacao congelados;
- identificar personas e semanas com regressao;
- gerar relatorio tecnico em JSON e Markdown;
- preparar a entrada de guardrails do Dia 30.

Este marco nao pode:

- recalibrar V2;
- criar V2.1;
- selecionar casos para revisao cega final;
- executar reserva;
- promover modelo;
- integrar V2 ao fluxo real;
- declarar a Appono.AI validada comercialmente;
- declarar `IA_PRONTA_EM_HOMOLOGACAO`.

## Fase 1 - Inventario e integridade

Registre:

- branch, `HEAD` e estado do Git;
- versoes de Node.js e npm;
- hashes dos relatorios brutos;
- hashes dos snapshots;
- hashes de personas, particoes e protocolo;
- contagens por conjunto e modelo;
- chaves duplicadas ou ausentes;
- campos nulos por metrica;
- estado da reserva;
- estado das feature flags.

Use como chave unica de decisao:

```text
dataset_id + scenario_id + model_version
```

Valide que:

- existem exatamente 900 registros por conjunto;
- existem exatamente 300 registros por modelo e conjunto;
- existem exatamente tres modelos por cenario;
- existem exatamente 30 decisoes por persona e modelo;
- semanas variam de 0 a 5;
- dias variam de 0 a 4;
- `scenario_index` varia de 0 a 29 dentro da persona;
- toda escolha nativa nao nula pertence aos candidatos elegiveis;
- todos os hashes comuns coincidem entre modelos no mesmo cenario;
- nenhum registro referencia conjunto de reserva;
- nenhum registro possui formula ou versao desconhecida.

## Fase 2 - Contrato do agregador

Crie um modulo puro e versionado, por exemplo:

`backend/src/domain/routine-intelligence-longitudinal-metrics.js`

Versao sugerida:

`routine-longitudinal-metrics-v1`

O modulo pode receber:

- relatorio bruto validado;
- snapshot correspondente;
- artefato de personas;
- manifesto de aceitacao congelado;
- configuracao estatistica versionada.

O modulo nao pode importar:

- controle, V1 ou V2;
- executor de ranking;
- politica de rollout;
- banco, HTTP ou Supabase;
- reserva;
- relatorio historico como fonte de calibracao;
- relogio real.

Exporte funcoes puras para:

- validar o relatorio bruto;
- indexar snapshots;
- juntar decisao e candidato;
- calcular estatisticas descritivas;
- agregar por dimensao;
- calcular diversidade e concentracao;
- comparar modelos por cenario;
- avaliar criterios congelados;
- construir relatorio canonico;
- serializar e calcular hash.

## Fase 3 - Juncao com snapshots

Os relatorios brutos guardam `native_choice_candidate_id`, mas nao repetem restaurante, produto e categoria. Recupere esses atributos exclusivamente do snapshot correspondente.

Construa indices por:

```text
scenario_id -> candidate_id -> candidate
```

Para cada decisao nativa bem-sucedida, exija correspondencia exata de:

- conjunto;
- cenario;
- candidato;
- restaurante sintetico;
- produto sintetico;
- categoria;
- preco;
- distancia;
- elegibilidade.

Falhe quando:

- cenario nao existir;
- candidato nao existir;
- candidato for inelegivel;
- houver duplicidade;
- conjunto divergir;
- hash do snapshot divergir;
- escolha nativa for nula sem erro tecnico;
- fallback aparecer como escolha nativa.

Nao copie catalogos completos para os relatorios agregados. Mantenha somente identificadores sinteticos e atributos agregaveis necessarios.

## Fase 4 - Populacoes e denominadores

Defina explicitamente as populacoes:

### Decisoes nativas

Registros com:

- `technical_error_code` nulo;
- `native_choice_candidate_id` nao nulo;
- `fallback_used` falso.

Sao o denominador principal de utilidade, arrependimento, diversidade e concentracao.

### Decisoes com reacao elegivel

Decisoes nativas de personas com `consentimento_sintetico: true` e `reaction_type` nao nulo.

Sao o denominador das taxas de aprovacao, recusa, alternativa, edicao e conversao simulada.

### Grupo sem historico

Deve ser reportado separadamente. Ausencia de reacao ou sinal nao pode ser tratada como recusa, aprovacao ou dado faltante.

### Falhas e fallbacks

Devem ser contados em metricas operacionais separadas. Nunca entram como acerto, utilidade escolhida ou cobertura de preferencia do modelo que falhou.

Registre numerador, denominador e taxa para toda metrica percentual.

## Fase 5 - Estatisticas basicas

Implemente funcoes deterministicas para:

- contagem;
- soma;
- media;
- mediana;
- minimo;
- maximo;
- desvio padrao populacional;
- percentis 10, 25, 75 e 90;
- proporcao;
- diferenca absoluta;
- diferenca relativa quando o denominador for diferente de zero.

Congele o metodo de percentil antes de observar resultados. Use um metodo simples e documentado, como nearest-rank ou interpolacao linear. O mesmo metodo deve ser usado em todos os conjuntos.

Arredonde somente na apresentacao. Calcule internamente com precisao integral de `Number` finito e documente tolerancia numerica.

## Fase 6 - Utilidade externa

Para cada conjunto, modelo, persona e semana, calcule sobre `chosen_external_utility`:

- quantidade valida;
- media;
- mediana;
- minimo;
- maximo;
- desvio padrao;
- percentis 10 e 90;
- estabilidade entre semanas.

Calcule tambem a diferenca por cenario:

```text
delta_utilidade_v2_controle = utilidade_v2 - utilidade_controle
delta_utilidade_v2_v1 = utilidade_v2 - utilidade_v1
```

Classifique com tolerancia congelada, por exemplo `1e-9`:

- vitoria: delta maior que a tolerancia;
- derrota: delta menor que o negativo da tolerancia;
- empate: valor absoluto dentro da tolerancia.

Nao use score total para essa classificacao.

## Fase 7 - Arrependimento

Use o campo bruto `regret`, validando:

```text
regret = best_external_utility - chosen_external_utility
```

Calcule por conjunto, modelo, persona e semana:

- media;
- mediana;
- maximo;
- percentis 75 e 90;
- quantidade acima dos limiares relevantes;
- diferenca V2 menos controle;
- diferenca V2 menos V1.

Menor arrependimento e melhor.

Calcule a regressao de persona como:

```text
regressao_persona_v2_controle = arrependimento_medio_v2 - arrependimento_medio_controle
```

Registre a pior persona, a pior semana e o pior cenario. O criterio congelado de regressao maxima por persona e `2,0`.

Nao esconda regressao positiva grave por meio de melhora em outra persona.

## Fase 8 - Reacoes sinteticas

Calcule por conjunto, modelo, persona e semana:

- taxa de `CONVERSAO_SIMULADA`;
- taxa de `APROVACAO`;
- taxa de `EDICAO`;
- taxa de `ALTERNATIVA`;
- taxa de `RECUSA`;
- taxa combinada de resposta positiva, declarada como metrica sintetica;
- distribuicao absoluta de reacoes.

Definicao sugerida de resposta positiva:

```text
CONVERSAO_SIMULADA ou APROVACAO
```

Nao inclua `EDICAO` como positiva sem justificativa pre-registrada. Nao chame `CONVERSAO_SIMULADA` de venda, pedido ou reserva.

## Fase 9 - Diversidade

Depois da juncao com snapshots, calcule por conjunto, modelo, persona e semana:

- restaurantes distintos;
- produtos distintos;
- categorias distintas;
- proporcao de opcoes distintas sobre decisoes nativas;
- novidade em relacao as escolhas anteriores da mesma ramificacao;
- cobertura acumulada ao longo das seis semanas.

Calcule tambem no conjunto completo por modelo:

- total de restaurantes sinteticos distintos;
- total de produtos sinteticos distintos;
- total de categorias distintas;
- media e minimo de diversidade por persona.

Nao compare apenas contagem global: um modelo pode parecer diverso por variar entre personas e ainda repetir excessivamente dentro de cada persona.

## Fase 10 - Repeticao

Calcule por ramificacao temporal:

- repeticao consecutiva de restaurante;
- repeticao consecutiva de produto;
- repeticao consecutiva de categoria;
- maior sequencia consecutiva;
- proporcao de repeticoes;
- repeticao dentro da mesma semana;
- repeticao entre a ultima decisao de uma semana e a primeira da seguinte.

Use a ordem congelada:

```text
persona + virtual_week + virtual_day + scenario_index
```

Nao ordene por `scenario_id` ou candidato.

## Fase 11 - Concentracao

Calcule separadamente para restaurante, produto e categoria:

- frequencia maxima;
- participacao maxima;
- indice Herfindahl-Hirschman, definido como soma dos quadrados das participacoes;
- quantidade de itens com participacao acima de 20%, quando aplicavel.

Calcule por:

- conjunto e modelo;
- conjunto, modelo e persona;
- conjunto, modelo, persona e semana quando houver amostra suficiente.

O criterio congelado exige concentracao da V2 nao pior que a V1. Declare previamente qual medida primaria sera usada. Recomendacao: participacao maxima por persona, com HHI como diagnostico secundario.

Nao escolha depois dos resultados a medida que favorece a V2.

## Fase 12 - Preferencias explicitas

Calcule cobertura somente nos cenarios em que pelo menos um candidato elegivel pertence a uma categoria explicitamente preferida pela persona.

Para cada modelo, registre:

- cenarios com oportunidade de preferencia;
- escolhas dentro da preferencia;
- cobertura percentual;
- cobertura por persona;
- cobertura por semana;
- diferenca V2 menos controle;
- diferenca V2 menos V1.

Personas sem preferencias explicitas ficam fora do denominador e aparecem como `not_applicable`, nao zero.

O criterio congelado de margem de regressao da preferencia explicita e `0`: a V2 nao pode ficar abaixo do controle nessa cobertura.

## Fase 13 - Confianca e evidencia

Calcule para V1 e V2, preservando `null` quando a metrica nao se aplica:

- confianca media e mediana;
- minimo, maximo e percentis;
- amostras efetivas medias;
- volume efetivo medio quando disponivel;
- consistencia media quando disponivel;
- confianca por semana;
- confianca por persona;
- confianca no grupo sem historico;
- proporcao de decisoes acima de `0,25`;
- confianca em cenarios com uma, poucas e muitas amostras;
- confianca em persona contraditoria.

O limiar `0,25` e um requisito operacional interno congelado, nao prova de calibracao estatistica.

Crie faixas predefinidas:

- `[0, 0.25)`;
- `[0.25, 0.50)`;
- `[0.50, 0.75)`;
- `[0.75, 1.00]`.

Para cada faixa, reporte quantidade e taxa sintetica de resposta positiva. Nao declare calibracao real com base em personas sinteticas.

## Fase 14 - Estabilidade semanal

Para cada modelo e persona, calcule:

- utilidade media por semana;
- arrependimento medio por semana;
- taxa positiva por semana;
- diversidade por semana;
- concentracao por semana;
- confianca por semana;
- maior variacao absoluta entre semanas consecutivas;
- desvio padrao entre semanas.

Marque mudancas esperadas da persona `mudanca_gradual` separadamente. Nao classifique automaticamente uma transicao programada como instabilidade do modelo.

## Fase 15 - Comparacao pareada

Compare modelos somente em registros que compartilham:

- conjunto;
- cenario;
- persona;
- semana;
- entrada comum valida;
- escolha nativa bem-sucedida.

Produza as matrizes:

- controle versus V1;
- controle versus V2;
- V1 versus V2.

Para cada matriz, registre:

- concordancias de escolha;
- divergencias;
- vitorias de utilidade da esquerda;
- vitorias de utilidade da direita;
- empates de utilidade;
- delta medio e mediano de utilidade;
- delta medio e mediano de arrependimento;
- quantidade de pares excluidos e motivo.

Como as trajetorias podem possuir historicos diferentes, descreva a comparacao como avaliacao pareada por contexto e regua externa, nao como experimento online randomizado.

## Fase 16 - Criterios congelados

Leia os criterios de `backend/experiments/routine-intelligence/manifest.json` e avalie, sem alteracao:

- `eliminatory_violations = 0`;
- determinismo verdadeiro;
- neutralidade sem historico verdadeira;
- arrependimento da V2 nao pior que V1;
- arrependimento da V2 nao pior que controle;
- regressao maxima por persona menor ou igual a `2,0`;
- margem de regressao de preferencia explicita igual a `0`;
- concentracao da V2 nao pior que V1;
- confianca minima interna `0,25` para decisao futura;
- consentimento ativo obrigatorio;
- fallback para erro, baixa confianca ou ausencia de historico.

Para cada criterio, retorne:

- identificador;
- valor esperado;
- valor observado;
- conjunto avaliado;
- estado `PASS`, `FAIL`, `NOT_APPLICABLE` ou `INSUFFICIENT_EVIDENCE`;
- evidencia rastreavel;
- observacao metodologica.

Validacao deve ser a fonte primaria para aceitacao. Desenvolvimento pode explicar defeitos, mas nao substituir falha de validacao.

## Fase 17 - Regressoes por persona

Produza uma tabela por persona contendo, para os tres modelos:

- utilidade media e mediana;
- arrependimento medio, mediano e maximo;
- resposta positiva;
- diversidade de restaurante, produto e categoria;
- concentracao maxima;
- repeticao consecutiva;
- cobertura de preferencia explicita;
- confianca e consistencia quando aplicavel;
- vitorias, derrotas e empates da V2 contra controle e V1.

Destaque:

- pior regressao da V2 contra controle;
- pior regressao da V2 contra V1;
- persona com maior concentracao;
- persona com menor cobertura de preferencia;
- persona com confianca potencialmente excessiva;
- grupo sem historico;
- sinais contraditorios;
- mudanca gradual;
- aversao a repeticao.

Uma media global positiva nao pode apagar um `FAIL` grave de persona.

## Fase 18 - Qualidade dos dados

Produza um bloco de auditoria com:

- completude dos registros;
- unicidade das chaves;
- sucesso da juncao com snapshots;
- nulos por campo;
- valores nao finitos;
- arrependimento negativo;
- confianca fora de `[0, 1]`;
- ajuste V2 fora de `[-8, 8]`;
- escolhas inelegiveis;
- hashes comuns divergentes;
- falhas e fallbacks;
- semanas ou personas ausentes;
- reacoes desconhecidas;
- campos proibidos ou PII;
- diferenca entre totais brutos e agregados.

Qualquer defeito que altere denominador ou conclusao deve bloquear o marco ate ser explicado ou corrigido na camada de avaliacao.

## Fase 19 - Relatorios de saida

Crie artefatos novos, sem alterar os relatorios brutos:

- `backend/reports/routine-intelligence/prospective/metrics-desenvolvimento-v1.json`;
- `backend/reports/routine-intelligence/prospective/metrics-validacao-v1.json`;
- `backend/reports/routine-intelligence/prospective/comparison-v1.json`;
- `docs/appono-intelligence-v2-comparativo-2026-09-29.md`.

Schema sugerido:

`routine-longitudinal-metrics-report-v1`

Cada JSON deve registrar:

- schema e versao do agregador;
- hashes das entradas;
- configuracao estatistica;
- definicoes e denominadores;
- qualidade dos dados;
- metricas globais;
- metricas por modelo;
- metricas por persona;
- metricas por semana;
- comparacoes pareadas;
- criterios congelados;
- limitacoes;
- hash canonico do conteudo.

O Markdown deve ser derivado dos JSONs, nao calculado manualmente com numeros diferentes.

## Fase 20 - CLI de avaliacao

Crie uma CLI local, por exemplo:

`backend/scripts/evaluate-routine-intelligence-longitudinal.js`

Script sugerido:

```text
evaluate:rotina:longitudinal
```

A CLI deve:

- aceitar `desenvolvimento_v1`, `validacao_v1` ou `all`;
- recusar qualquer argumento contendo `reserva`;
- verificar hashes antes de avaliar;
- exigir `--write` para criar relatorios;
- oferecer `--check` sem escrita;
- oferecer `--help` sem escrita;
- usar destinos predefinidos;
- impedir sobrescrita incompativel;
- escrever atomicamente;
- imprimir somente resumo agregado;
- retornar codigo diferente de zero em falha;
- nao ler `.env`;
- nao executar modelos;
- nao importar formulas de ranking.

Resumo permitido na CLI:

- conjunto;
- registros lidos;
- registros agregados;
- falhas de qualidade;
- criterios `PASS`, `FAIL` e `INSUFFICIENT_EVIDENCE`;
- hashes dos relatorios;
- reserva acessada: falso.

## Fase 21 - Ordem de execucao

Siga esta ordem:

1. valide hashes e contagens brutas;
2. congele definicoes estatisticas e tolerancias;
3. implemente agregador e testes com fixtures pequenas;
4. execute avaliacao de desenvolvimento;
5. investigue somente defeitos de agregacao ou dados;
6. nao altere formulas nem criterios;
7. congele o agregador por hash;
8. execute avaliacao de validacao;
9. gere comparacao conjunta mantendo blocos separados;
10. execute `--check` para provar reproducibilidade;
11. escreva relatorio Markdown a partir dos JSONs;
12. execute suite, builds, lint e diff;
13. produza checkpoint e atualize somente 29/09 no calendario.

Resultado desfavoravel da V2 nao e defeito do agregador.

## Fase 22 - Testes obrigatorios

Adicione testes para comprovar:

- hashes brutos conferem com o manifesto;
- exatamente 900 registros existem por conjunto;
- chave composta e unica;
- existem tres modelos por cenario;
- juncao com snapshot possui cobertura de 100%;
- candidato escolhido e elegivel;
- fallback nao entra como escolha nativa;
- desenvolvimento e validacao nao se misturam;
- media, mediana, percentis e desvio padrao estao corretos;
- tolerancia de empate e deterministica;
- utilidade e arrependimento usam decisoes nativas;
- arrependimento e validado contra melhor utilidade;
- taxas registram numerador e denominador;
- grupo sem historico nao entra no denominador de reacao;
- diversidade usa restaurante, produto e categoria do snapshot;
- repeticao respeita ordem temporal;
- concentracao maxima e HHI estao corretos;
- cobertura explicita usa apenas cenarios com oportunidade;
- persona sem preferencia retorna `not_applicable`;
- confianca nula permanece nula;
- faixas de confianca nao se sobrepoem;
- V2 sem historico possui confianca zero;
- regressao maxima por persona nao e escondida;
- criterios congelados nao podem ser sobrescritos por argumento;
- relatorio de validacao e fonte primaria de aceitacao;
- relatorios sao canonicos e deterministicos;
- mudanca de entrada altera hash;
- ordem de chaves nao altera hash canonico;
- `--help` e `--check` nao escrevem;
- reserva e recusada;
- agregador nao importa modelos, banco ou HTTP;
- saidas nao contem PII ou segredos;
- relatorios brutos e snapshots permanecem inalterados;
- rollout publico permanece zero.

## Fase 23 - Testes de mutacao

Garanta falha explicita para:

- hash bruto divergente;
- relatorio truncado;
- registro duplicado;
- modelo ausente;
- modelo desconhecido;
- cenario ausente;
- candidato inexistente;
- candidato inelegivel escolhido;
- conjunto divergente;
- utilidade nao finita;
- arrependimento negativo;
- confianca fora do intervalo;
- ajuste V2 fora do limite;
- fallback creditado como escolha;
- semana ausente;
- persona desconhecida;
- denominador zero tratado como taxa zero;
- tentativa de misturar conjuntos;
- criterio de aceitacao alterado;
- tentativa de reserva;
- destino de escrita historico;
- campo desconhecido no relatorio agregado.

Mensagens devem usar codigos tecnicos seguros e nao imprimir registros completos.

## Fase 24 - Verificacao obrigatoria

Descubra primeiro os comandos reais. Execute os equivalentes a:

```text
npm.cmd test --workspace backend
npm.cmd run build --workspace backend
npm.cmd run lint --workspace frontend
npm.cmd run build --workspace frontend
git diff --check
```

Execute tambem:

```text
npm.cmd run simulate:rotina:longitudinal --workspace backend -- --dataset=desenvolvimento_v1 --check
npm.cmd run simulate:rotina:longitudinal --workspace backend -- --dataset=validacao_v1 --check
npm.cmd run audit:rotina:scenarios --workspace backend
npm.cmd run audit:rotina:partitions --workspace backend
```

Depois de implementar a avaliacao, execute os comandos reais equivalentes a:

```text
npm.cmd run evaluate:rotina:longitudinal --workspace backend -- --dataset=desenvolvimento_v1 --write
npm.cmd run evaluate:rotina:longitudinal --workspace backend -- --dataset=desenvolvimento_v1 --check
npm.cmd run evaluate:rotina:longitudinal --workspace backend -- --dataset=validacao_v1 --write
npm.cmd run evaluate:rotina:longitudinal --workspace backend -- --dataset=validacao_v1 --check
npm.cmd run evaluate:rotina:longitudinal --workspace backend -- --dataset=all --check
```

Rode os testes focados do agregador pelo menos tres vezes.

Nao execute:

- modelos em modo `--write`;
- simulacao historica;
- reserva historica ou prospectiva;
- scripts sombra remotos;
- Supabase remoto;
- seed, pagamento, e-mail ou deploy.

## Fase 25 - Relatorio comparativo humano

Crie `docs/appono-intelligence-v2-comparativo-2026-09-29.md` com:

1. escopo e pergunta respondida;
2. fontes e hashes;
3. definicoes de metricas;
4. qualidade dos dados;
5. resultados de desenvolvimento;
6. resultados de validacao;
7. tabela global por modelo;
8. tabela por persona;
9. tabela por semana;
10. comparacoes pareadas;
11. diversidade, repeticao e concentracao;
12. preferencias explicitas;
13. confianca e evidencia;
14. criterios congelados;
15. regressao maxima;
16. principais desacordos a investigar;
17. limitacoes sinteticas;
18. conclusao tecnica do marco;
19. entrada para guardrails de 30/09.

Use `Appono.AI` como nome publico no texto explicativo. Preserve os identificadores tecnicos dos modelos nas tabelas e evidencias.

## Fase 26 - Checkpoint do Dia 29

Crie:

`docs/appono-intelligence-v2-checkpoint-2026-09-29.md`

O checkpoint deve registrar:

1. decisao do dia;
2. data planejada e real;
3. pre-condicoes verificadas;
4. `HEAD` e estado inicial do Git;
5. hashes das entradas;
6. versao e hash do agregador;
7. definicoes e denominadores congelados;
8. qualidade dos dados;
9. metricas globais por conjunto e modelo;
10. resultados por persona;
11. resultados por semana;
12. comparacoes pareadas;
13. diversidade e concentracao;
14. cobertura de preferencia explicita;
15. confianca e consistencia;
16. criterios `PASS`, `FAIL` ou sem evidencia;
17. pior regressao observada;
18. arquivos criados ou alterados;
19. comandos e resultados;
20. hashes das saidas;
21. confirmacao de que formulas nao mudaram;
22. confirmacao de que relatorios brutos nao mudaram;
23. confirmacao de que nenhuma reserva foi acessada;
24. confirmacao de rollout zero;
25. limitacoes;
26. itens preparados para 30/09;
27. entrada exata para regressao e guardrails.

Atualize o calendario somente depois de cumprir os criterios. Marque apenas 29/09 como concluido.

## Criterios de encerramento de 29/09

O marco esta concluido somente quando:

- Dia 28 continuar aprovado e verificavel;
- relatorios brutos mantiverem seus hashes;
- agregador for puro e independente dos modelos;
- juncao com snapshots tiver cobertura de 100%;
- metricas tiverem numeradores e denominadores explicitos;
- desenvolvimento e validacao permanecerem separados;
- utilidade e arrependimento forem calculados por modelo, persona e semana;
- diversidade, repeticao e concentracao estiverem disponiveis;
- preferencias explicitas tiverem cobertura corretamente condicionada;
- confianca, volume e consistencia preservarem valores ausentes;
- comparacoes pareadas explicarem exclusoes;
- pior regressao por persona estiver visivel;
- criterios congelados forem avaliados sem alteracao;
- relatorios JSON e Markdown forem reproduziveis;
- testes focados passarem tres vezes;
- suite, builds, lint e `git diff --check` passarem;
- nenhum snapshot, relatorio bruto ou artefato historico for alterado;
- reserva prospectiva continuar selada;
- rollout publico permanecer zero;
- checkpoint de 29/09 estiver completo;
- nenhuma operacao remota, commit, push ou deploy ocorrer.

## Decisao permitida hoje

Use uma destas decisoes:

- `METRICAS_COMPARATIVAS_CONCLUIDAS`: dados integros, agregacao reproduzivel e criterios avaliados;
- `METRICAS_COMPARATIVAS_PARCIAIS`: metricas principais existem, mas ha lacuna de qualidade, denominador ou reproducibilidade;
- `METRICAS_COMPARATIVAS_BLOQUEADAS`: dados ou metodologia impedem comparacao confiavel.

O relatorio pode registrar criterios aprovados ou reprovados. Nao declare V2.1 aprovada, piloto autorizado ou IA pronta em homologacao neste marco.

## Aceleracao segura

Depois de concluir o Dia 29, pode ficar marcado como `PREPARADO` para 30/09:

- lista de personas com regressao;
- lista de semanas instaveis;
- casos de preferencia explicita perdida;
- casos de pouca evidencia e confianca alta;
- casos contraditorios;
- repeticoes excessivas;
- falhas de diversidade ou concentracao;
- fixtures minimas para testes de regressao;
- matriz de guardrails por criterio.

Nao pode ficar marcado como concluido:

- alteracao da V2;
- V2.1;
- revisao humana cega;
- reserva;
- integracao real;
- homologacao ou release.

## Entrega final

Ao terminar, apresente:

1. decisao do dia;
2. confirmacao de encerramento do Dia 28;
3. arquivos criados ou alterados;
4. versao e hash do agregador;
5. qualidade e completude dos dados;
6. definicoes das metricas;
7. resultados de desenvolvimento;
8. resultados de validacao;
9. comparacao controle versus V1 versus V2;
10. resultados por persona e semana;
11. diversidade, repeticao e concentracao;
12. cobertura de preferencia explicita;
13. confianca, volume e consistencia;
14. vitorias, derrotas e empates pareados;
15. criterios congelados aprovados e reprovados;
16. pior regressao encontrada;
17. testes, builds, lint e diff;
18. hashes preservados e novos;
19. confirmacao de que nenhuma formula mudou;
20. confirmacao de que nenhuma reserva foi acessada;
21. confirmacao de rollout publico zero;
22. limitacoes por ausencia de clientes reais;
23. trabalho preparado para 30/09;
24. entrada exata do proximo marco: transformar regressoes observadas em testes de guardrail sem recalibrar oportunisticamente a V2.

Nao confunda score interno com qualidade, confianca com acerto, divergencia com melhoria, media global com seguranca por persona ou simulacao com evidencia de mercado. O objetivo de 29/09 e transformar os resultados brutos da Appono.AI em uma comparacao auditavel que mostre tanto ganhos quanto regressoes antes de qualquer mudanca de modelo.
